# v4.1 数据

沿用 v4.0 生成规则，使用独立种子 20260916。低精度端点改为 FP4，恢复需求字段为 `min_fp4_gap`。

运行输入保存在 `runs/v41-*/construction` 和 `runs/v41-*/inputs/data/v4.1/`，正式数据副本保存在 `data/v4.1/v41-*/quality`。发现、两批确认、实验、测试保持实例分离。
