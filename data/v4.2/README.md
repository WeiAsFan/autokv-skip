# v4.2 冻结数据

直接复用 v4.1 运行 `v41-40ef467cef6a32d2` 的 `single_lookup-l8192-r128`：512 条实验题与 4096 条尚未使用测试题。种子为 `20260916`，不重新生成或按答案筛题。

使用 `bash scripts/run-v42.sh --reuse-run /旧项目/runs/v41-40ef467cef6a32d2` 导入。实际文件保存在新运行的 `inputs/data/v4.2/<run-id>/quality/`，后续续跑无需旧目录或原来源库。原数据文件不作为本次代码提交新增内容。

网页上传只提交精简分析包：包含逐题真值、短回答、分层与来源组等复核信息。长提示和完整原始运行保留在服务器。来源与许可沿用 v4.1 数据清单及 [v3.0 来源说明](../v3.0/README.md)。
