# v4.2 验证记录

日期：2026-09-17。本地 Windows、Python 3.11；GPU 服务和 tokenizer 在流程测试中用可控替身替换，实际运行器、逐题缓存、评分、报告和导出仍使用生产实现。

最终全量回归：**231 项全部通过，无跳过**，包括原有 217 项和本次新增 14 项。最终一轮耗时约 120 秒。

## 搜索与流程验证

- 全部候选初筛同分时，仍推进到第 9 层；每层最多 8 个复筛候选，全程最多 6 个完整候选，搜索请求不超过 24320。
- 早期复筛误判只消耗 3 个提前确认名额，末尾仍能确认较深层数的可行组合。
- 第二条父路径可保留条件性有效的组合；完整实验集确认后才冻结。
- 完整评估中途失败后，重放相同路径并补齐缺失题，不新增第七个候选，不重复成功回答。
- 请求预算耗尽时，部分回答不能被当成达标候选；P0 已达标或 BF16 失效时保持端点语义。
- 数据原样导入，不调用构造阶段；首次入口保存实际输入，旧数据目录移走后仍可续跑已导入的运行。
- 测试失败只保留唯一冻结候选；原测试已使用时标记为诊断，不计主实验达标。
- 传递 v4.1 正式运行的兼容路径、cubin 和编译缓存设置；精简导出保留中断现场并记录坏行，不修改源文件。

历史 v3/v4.0/v4.1 的协议、并发和 FP4 数值测试一并回归。Windows 原默认临时路径混用 `ADMINI~1` 与完整用户名，曾触发两个旧 CLI 测试错误；将测试进程的临时目录规范化后重跑，未改动那些历史实验代码。PyTorch 数值测试使用已经准备的 `.cache/v41-test-deps`，没有新增服务器依赖。

Linux 已有 PyTorch 环境可直接执行：

```bash
python -m unittest discover -s tests -q
```

本地 Windows 的对应命令为：

```powershell
$env:PYTHONUTF8='1'
$env:PYTHONPATH=(Join-Path (Get-Location) '.cache/v41-test-deps')
python -c "import sys,tempfile,unittest; from pathlib import Path; tempfile.tempdir=str(Path(tempfile.gettempdir()).resolve()); result=unittest.TextTestRunner().run(unittest.defaultTestLoader.discover('tests')); sys.exit(not result.wasSuccessful())"
```

## v4.1 真实文件验证

使用已上传的运行 `v41-40ef467cef6a32d2`，直接导入得到：512 条 experiment、4096 条 test，条件仍为 `single_lookup-l8192-r128`；源运行没有 test 回答。导入后的配置容量目标为 2.0×，允许最多 9 层 BF16。该验证只读取原数据，不执行模型推理或查看新的测试输出。

将同一运行转换为精简分析包，并仅从包内样本真值与短回答重新评分：

| 项目 | 结果 |
|---|---:|
| 原归档分片总字节 | 147587765 |
| 分析 ZIP 字节 | 6818870 |
| 连同说明和报告的导出总字节 | 7125107 |
| 样本索引（含旧发现与确认） | 8128 |
| 回答条数 | 32640 |
| 重新评分不一致 | 0 |
| 服务尝试记录 | 222 |
| 还原的总请求数 | 32640 |
| 还原的累计尝试耗时（秒） | 60173.7085 |
| 导出坏行/文件数 | 0 |

分析 ZIP 约为原归档的 4.6%，没有丢掉已保存回答；精简的是长提示、重复请求 ID 数组、完整日志等。这个大小来自 v4.1 实际结果转换，不承诺所有未来运行具有相同比例。

已检查 `run-v42.sh` 的 Bash 语法和两个 CLI 的帮助入口。尚未执行 v4.2 的 A6000 正式质量、容量和耗时实验；CPU 测试、旧结果转换与真实题库导入不能代替这些 GPU 结果。
